# 异步图片任务后台服务 · 部署说明

> 本文档面向后端/运维同学。前端（`https://xym.aittco.com`）已上线，需要部署一个配套的 Node 后台服务，否则前端生图会报 `POST /api/image-tasks/generations 404`。

---

## 一、背景与问题

前端改版后，生图不再由浏览器直接请求上游（`vip.aittco.com`），而是改成「先提交任务给后台服务 → 后台异步调用上游生图 → 前端轮询取结果」。这样能解决浏览器长连接中断后收不到已扣费图片的问题。

当前前端请求 `https://xym.aittco.com/api/image-tasks/generations` 返回 **404**，原因是 `/api/image-tasks/` 这个路径没有被反向代理到本服务（或本服务尚未部署）。

---

## 二、服务概览

| 项 | 值 |
|---|---|
| 服务名 | chaoqing-image-task-server |
| 入口文件 | `server.mjs` |
| 运行环境 | Node.js **>= 14.17.6**（已做兼容，无需 Node 18/20） |
| 监听地址 | `127.0.0.1:3366`（只监听内网，不直接暴露公网） |
| 包管理器 | npm（依赖已在 `package.json` 中精确锁定，无需 lock 文件） |
| 依赖 | abort-controller、busboy、form-data、node-fetch（版本已精确锁定） |
| 数据持久化 | 本地文件（任务元数据 + 生成图片），默认保存 24 小时 |

---

## 三、需要部署的文件

只需上传 `server/` 目录下的这 3 个文件（**不要**上传 `node_modules`，服务器上会重新安装）：

```text
server/
├── server.mjs          # 服务主程序（必须）
├── package.json        # 依赖声明（必须，依赖版本已精确锁定）
└── server.test.mjs     # 自测脚本（可选）
```

建议放到服务器目录 `/opt/chaoqing-server/`。

---

## 四、部署步骤

```bash
# 1. 把上面 3 个文件上传到 /opt/chaoqing-server/

# 2. 进入目录，安装依赖（只装生产依赖）
cd /opt/chaoqing-server
npm install --production

# 3. 启动（先手动验证，后面用 systemd 常驻）
npm start
```

启动后先自测服务本身：

```bash
curl http://127.0.0.1:3366/api/image-tasks/health
# 期望返回：{"ok":true,"active":0}
```

---

## 五、常驻运行（systemd 示例）

不要用 `npm start` 挂前台，建议用 systemd 托管。创建 `/etc/systemd/system/chaoqing-image-task.service`：

```ini
[Unit]
Description=Chaoqing Image Task Server
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/chaoqing-server
ExecStart=/usr/bin/node server.mjs
Restart=always
RestartSec=3

# 环境变量（详见第七节）
Environment=NODE_ENV=production
Environment=PORT=3366
Environment=HOST=127.0.0.1
Environment=FRONTEND_ORIGIN=https://xym.aittco.com
Environment=UPSTREAM_IMAGE_BASE=https://vip.aittco.com/v1
Environment=UPSTREAM_IMAGE_MODEL=gpt-image-2
Environment=IMAGE_TASK_DATA_DIR=/var/lib/chaoqing-image-tasks

# 按实际运行用户调整，注意数据目录要有写权限
User=www-data
Group=www-data

[Install]
WantedBy=multi-user.target
```

启用并启动：

```bash
mkdir -p /var/lib/chaoqing-image-tasks
chown www-data:www-data /var/lib/chaoqing-image-tasks
systemctl daemon-reload
systemctl enable --now chaoqing-image-task
systemctl status chaoqing-image-task
```

---

## 六、Nginx 反向代理配置

在 `xym.aittco.com` 的 Nginx 配置中加入：

```nginx
location /api/image-tasks/ {
    proxy_pass http://127.0.0.1:3366;   # 注意：末尾不要加斜杠，否则路径会被改写导致路由不匹配
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_connect_timeout 30s;
    proxy_send_timeout 900s;            # 生图耗时较长，超时需放宽
    proxy_read_timeout 900s;
    client_max_body_size 64m;           # 图片编辑上传最大 64MB
}
```

重载：

```bash
nginx -t && nginx -s reload
```

---

## 七、环境变量说明

| 变量 | 默认值 | 生产建议 | 说明 |
|---|---|---|---|
| `PORT` | `3366` | `3366` | 服务监听端口 |
| `HOST` | `127.0.0.1` | `127.0.0.1` | 只监听本机，由 Nginx 转发 |
| `FRONTEND_ORIGIN` | 空 | `https://xym.aittco.com` | **必须设置**，用于校验请求来源（Origin） |
| `UPSTREAM_IMAGE_BASE` | `https://vip.aittco.com/v1` | 同默认 | 上游生图接口地址 |
| `UPSTREAM_IMAGE_MODEL` | `gpt-image-2` | 同默认 | 正式模型，**不要改** |
| `IMAGE_TASK_DATA_DIR` | `server 目录/data` | `/var/lib/chaoqing-image-tasks` | 任务与图片存储目录，建议放持久磁盘 |
| `TASK_RETENTION_MS` | `86400000`（24h） | 同默认 | 结果保留时长 |
| `MAX_ACTIVE_PER_IP` | `4` | 同默认 | 单 IP 并发任务上限 |
| `MAX_ACTIVE_TOTAL` | `12` | 同默认 | 全局并发任务上限 |
| `MAX_UPLOAD_BYTES` | `67108864`（64MB） | 同默认 | 上传图片大小上限 |

---

## 八、接口清单

| 方法 | 路径 | 用途 | 鉴权 |
|---|---|---|---|
| POST | `/api/image-tasks/generations` | 提交文生图任务 | Bearer（客户密钥）+ Idempotency-Key |
| POST | `/api/image-tasks/edits` | 提交图片编辑/线稿任务（multipart） | Bearer + Idempotency-Key |
| GET | `/api/image-tasks/{id}` | 查询任务状态 | 无 |
| GET | `/api/image-tasks/{id}/result` | 下载生成结果图片 | 无 |
| GET | `/api/image-tasks/health` | 健康检查 | 无 |

前端已按以上路径实现，服务端路由也已匹配，**保持路径原样转发即可**（这就是为什么 Nginx `proxy_pass` 末尾不能加斜杠）。

---

## 九、验证方法（部署完成后）

```bash
# 1. 本机服务健康检查
curl http://127.0.0.1:3366/api/image-tasks/health
# 期望：{"ok":true,"active":0}

# 2. 通过域名走完整链路
curl https://xym.aittco.com/api/image-tasks/health
# 期望：{"ok":true,"active":0}

# 3. 前端实际生成一张图，确认不再出现 404
```

如果第 2 步返回 404，说明 Nginx 代理没生效；返回 502，说明 Node 服务没起来；返回 `{"ok":true}` 即全链路正常。

---

## 十、注意事项

1. **服务只监听 `127.0.0.1`**，不要直接暴露公网，统一走 Nginx 转发。
2. **`FRONTEND_ORIGIN` 必须配置**为 `https://xym.aittco.com`，否则请求来源校验会失效。
3. **数据目录要持久化**（放非系统盘），任务元数据和图片存在这里，服务重启后靠它恢复。
4. **不要升级依赖版本**，`package.json` 里是经过 Node 14 兼容性验证的精确版本。
5. 客户密钥只存在于内存中，不会写入任务文件或日志。
6. 图片结果默认保留 24 小时后自动清理，如需调整改 `TASK_RETENTION_MS`。
